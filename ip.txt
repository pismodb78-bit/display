# ----------------------------------------------------------------------
#  Школьное расписание — настройки подключения.
#
#  Файл лежит рядом с SchoolSchedule.exe и правится обычным Блокнотом.
#  Программа сама замечает, что файл сохранён, и переподключается —
#  перезапускать .exe на телевизоре не нужно.
#
#  Строки, начинающиеся с #, — комментарии.
# ----------------------------------------------------------------------

# --- База данных (MAMP / phpMyAdmin) ---
server   = 192.168.0.20
port     = 3306
uid      = user1
password = scent01
database = school_schedule

# --- Пароль для входа в режим учителя ---
# Работает, пока пароль не сменили в самой программе
# (вкладка «Доступ»). После смены хранится в базе, а эта строка
# остаётся запасным входом, если пароль забыли: сотрите пароль
# в базе (таблица settings, строка admin_password) — снова заработает
# значение отсюда.
admin = 1234

# --- Экран ---
# theme      : dark — тёмная, light — светлая. Пусто — как выбрано в
#              программе (вкладка «Показ»); строка здесь перебивает выбор
#              только для этого компьютера
# fullscreen : 1 — во весь экран без рамки (телевизор), 0 — обычное окно
# scale      : масштаб шрифтов, 1.0 — обычный, 1.3 — крупнее на треть
# refresh    : как часто проверять базу на изменения, секунд
# theme    = dark
fullscreen = 1
scale      = 1.0
refresh    = 10
